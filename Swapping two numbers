#include<stdio.h>  
 int main()    
{    
    int x, y;      
    
    printf("Enter two numbers: ");
    scanf("%d%d", &x, &y) ;
    
    printf("before swapping\n First number= %d\n Second number= %d\n", x, y);
    
    
    x=x+y; //x=3 (1+2)    
    y=x-y; //y=1 (3-2)    
    x=x-y; //x=2(3-1)    


    printf("After swapping\n First number= %d\n Second number= %d\n", x, y);    
 
    
    return 0;  
}   
